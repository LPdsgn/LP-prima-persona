---
export interface Props {
  scripts: string[];
}
const { scripts } = Astro.props;
---

{ scripts && scripts.length > 0 &&
  scripts.map((script) => (
    <script src={script} type="text/javascript" defer />
  ))
}
